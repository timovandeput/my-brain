name: my_brain
description: Index and search a vault of markdown notes so an AI agent can use it as a second brain.
version: 0.1.0
license: MIT
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.5.0
  collection: ^1.18.0
  crypto: ^3.0.3
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
