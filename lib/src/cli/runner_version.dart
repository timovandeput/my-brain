/// Released version, reported by `my-brain version` and recorded in the
/// AGENTS.md that `init` writes into a vault.
///
/// Kept in its own file so `init` can read it without importing the runner
/// that registers `init` itself.
const String myBrainVersion = '0.1.0';
